<ul class="navbar_user">
    <li class="checkout">

        {% if is_granted('ROLE_USER') %}
        <a href="{{ path('panier') }}">
            <i class="fa fa-shopping-cart" aria-hidden="true"></i>

                <span id="checkout_items" class="checkout_items"> {{ articles }}</span>
           {% endif %}
        </a>
    </li>
</ul>